{
  "format_version": 2,
  "name": "ffrwd/wasm",
  "versions": [
    {
      "size": 5094,
      "sha256": "3e3344860b527abec868d94a6fbdd3403b119cc361be16c2cf6879bbd4ea2243",
      "license": "MIT",
      "recipes": [],
      "version": "0.9.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.9.0</code> carries <code>ffrwd:av@0.9.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.9.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-08-28T21:00:02.914827+00:00"
    },
    {
      "size": 5898,
      "sha256": "123bdfb59f3ac92cdd685c1cc9626e8063edea81d841135527add91e754f28b2",
      "license": "MIT",
      "recipes": [],
      "version": "0.10.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.9.0</code> carries <code>ffrwd:av@0.9.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.9.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-08-30T15:37:34.557185+00:00"
    },
    {
      "size": 6308,
      "sha256": "255b3d66e8ac40f3bf86530b78ca7358d1732c33b54c2810b1bb375ab6b6d172",
      "license": "MIT",
      "recipes": [],
      "version": "0.11.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.11.0</code> carries <code>ffrwd:av@0.11.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.11.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-08-31T16:26:47.811582+00:00"
    },
    {
      "size": 7389,
      "sha256": "00a56f4c1aee559c9608d596a53fb429a0347ad6db62f6ce3e1f64615a2f5bd9",
      "license": "MIT",
      "recipes": [],
      "version": "0.12.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.12.0</code> carries <code>ffrwd:av@0.12.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.12.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-09-01T18:50:41.780203+00:00"
    },
    {
      "size": 8009,
      "sha256": "cb0e78c8fbb2f066f649df6da3c1a25808eabc8fd7f21095d49b89c891adc788",
      "license": "MIT",
      "recipes": [],
      "version": "0.13.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.12.0</code> carries <code>ffrwd:av@0.12.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.12.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-09-03T19:46:32.483258+00:00"
    },
    {
      "size": 8408,
      "sha256": "4a638c3b7f5492add09467bb08ce3b48a6dfe65fed38966756e8579fb3419e9c",
      "license": "MIT",
      "recipes": [],
      "version": "0.14.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.12.0</code> carries <code>ffrwd:av@0.12.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.12.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-09-03T22:57:10.707461+00:00"
    },
    {
      "size": 8532,
      "sha256": "0942300729e217d46f5eb22504b920cfa8cf465e7e031307a636a1da7f9bc836",
      "license": "MIT",
      "recipes": [],
      "version": "0.15.0",
      "functions": [],
      "namespace": "ffrwd",
      "description": "The ffrwd:av wit world a wasm module is built against, carried as a package: this version's number is the world's. Assets only - no exports, no recipes, no module.",
      "readme_html": "<h1>ffrwd/wasm</h1>\n<p>The <code>ffrwd:av</code> wit world, versioned as this package is versioned:\n<code>ffrwd/wasm@0.12.0</code> carries <code>ffrwd:av@0.12.0</code>, immutable, resolvable\nforever. Depend on it and your build has the interface a module\ntargets — no vendored copy to drift, no path into anyone's checkout.</p>\n<h2>Using it from Rust</h2>\n<p>Declare the dependency and install it:</p>\n<pre><code>&quot;dependencies&quot;: { &quot;ffrwd/wasm&quot;: &quot;0.12.0&quot; }\n</code></pre>\n<pre><code>ffrwd install ffrwd/wasm\n</code></pre>\n<p>Then a <code>build.rs</code> puts the wit where <code>wit_bindgen::generate!</code> reads\nit:</p>\n<pre><code class=\"language-rust\">use std::{env, fs, path::PathBuf, process::Command};\n\nfn main() {\n    let source = match env::var_os(&quot;FFRWD_WIT_DIR&quot;) {\n        Some(dir) =&gt; PathBuf::from(dir),\n        None =&gt; {\n            let out = Command::new(&quot;ffrwd&quot;)\n                .args([&quot;path&quot;, &quot;ffrwd/wasm&quot;])\n                .output()\n                .expect(&quot;ffrwd on PATH&quot;);\n            assert!(out.status.success(), &quot;ffrwd path failed&quot;);\n            PathBuf::from(String::from_utf8(out.stdout).unwrap().trim()).join(&quot;wit&quot;)\n        }\n    }\n    .join(&quot;av.wit&quot;);\n    let wit = PathBuf::from(env::var(&quot;CARGO_MANIFEST_DIR&quot;).unwrap()).join(&quot;wit&quot;);\n    fs::create_dir_all(&amp;wit).unwrap();\n    fs::copy(&amp;source, wit.join(&quot;av.wit&quot;)).unwrap();\n}\n</code></pre>\n<p>with <code>wit_bindgen::generate!({ path: &quot;wit&quot; })</code> in the crate and\n<code>wit/</code> in <code>.gitignore</code>. <code>FFRWD_WIT_DIR</code> overrides the lookup for\nbuilds that already have the wit on disk.</p>\n<p>Or skip all of it: <code>ffrwd init --rust</code> scaffolds a working module —\nthis dependency, that build script, a buildable kernel, a recipe —\nready for <code>cargo build --target wasm32-wasip2 --release</code> and\n<code>ffrwd publish</code>.</p>\n<h2>Versions</h2>\n<p>A new world version is published here as a new package version, and\nolder versions stay: a module built against <code>0.8.0</code> keeps resolving\n<code>ffrwd/wasm@0.8.0</code> as long as the registry stands. The sidecar hosts\nevery world it ever shipped, so old modules keep running too.</p>\n",
      "capabilities": [],
      "engines_ffrwd": null,
      "homepage": null,
      "models": {},
      "yanked": false,
      "published_at": "2026-09-04T21:22:02.092651+00:00"
    }
  ]
}
